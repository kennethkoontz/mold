/* routes.js
 *
 * All routes are defined in this module. Routes map an incoming request to an
 * action method.
 *
 */

var routes = module.exports = {
    /* Example:
     *
     * '/foobar': 'foobar'
     *
     * A request for '/foobar' will call the 'foobar' action.
     *
     */
    '/': 'index'
}
